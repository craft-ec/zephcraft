# ZephCraft — State, Reconciliation & Roadmap

**Date:** 2026-07-07 · **Updated:** 2026-07-11
**Purpose:** the single consolidated view of where the node actually stands — what is built and validated, where the code and the *other* design docs disagree (a spec-vs-code reconciliation), what is deferred by choice, the known architectural constraints, and the prioritized plan. This is the current picture, not a changelog: it is kept edited-in-place. It supersedes ad-hoc status in conversation and complements `.claude/feature-progress.md` (the working phase tracker). For deep architecture, `ZEPHCRAFT.md` is the consolidated design doc.

---

## 1. Executive summary

The full vertical is standing and validated on live Hetzner hardware: **transport substrate → storage → database → compute → registry coordination**, with a real SQL app deployed, invoked, persisting, and surviving a node restart. The registry control-plane scaling ceiling (the previous #1 concern) is **resolved**, and the transport plane was rebuilt to a structural design and rolled to the fleet.

A spec-vs-code reconciliation (every concrete claim classified with file:line/commit evidence) reached a clear verdict that still holds:

- **The code is honest.** Every high-value "done" claim is confirmed in code + commits; the progress file, if anything, *understates*. No overstatement.
- **The debt is in the docs.** The largest remaining finding is stale documentation: `REGISTRY_DESIGN` (partially fixed), foundation §62/Part F, `GOVERNANCE §4/§6`, `CRAFTCOM_DESIGN`, and `ENCRYPTION §8` still describe **attestation / committee / quorum / Pkarr / tracker / fixed-shard** models the code replaced with an owner-signed CRDT + census-elected sharded registry + DHT. Anyone reading those builds the wrong mental model — this is the open Tier-0 sweep (§7).
- **A short list of genuine gaps** remains, mostly deferred by design and flagged in their own docs. (The one-time real robustness item, SWIM active death-detection, is now DONE + live — §5.)

**Highest-value single fix:** the doc-reconciliation sweep of the stale design docs above — chiefly the registry docs' non-existent *k-of-n attestation* security model.

---

## 2. Current state — built & validated

| Layer | Status | Notes |
|---|---|---|
| **Transport substrate (Transfer Plane v2)** | BUILT + LIVE | One QUIC connection per peer, each protocol a bi-stream with a 1-byte tag (mux; conns/node 24→7 at 8 nodes). Sender-side **bounded active set** (choke, K=4 distinct active-push peers, non-blocking) + **offer/grant admission** with class carried on every push and graded receiver gating from a `ResourceGauge`; repair redirects around a pressured peer. Elected healthscan (one scanner per cid per epoch) + class-fair `JobCoordinator`. Gated by an offline acceptance harness (`tests/tests/transfer_plane.rs` A–G) — nothing rolls without it (`deploy/gate.sh`). Spec: `docs/TRANSFER_PLANE_V2.md`. |
| **Storage (CraftOBJ)** | BUILT | Content-addressed (BLAKE3), RLNC erasure (k=8/n=32) + null-space vtags, health-scan repair/degrade/offload/fade, system objects (WANT-pinned), DHT-only routing. Publish retains locally + distributes in the background (fire-and-forget). ~52 MB/s local ingest. |
| **Database (CraftSQL)** | BUILT | SQLite VFS over content-addressed pages, single-writer-per-identity, generation-based durability (incremental diff per commit) + manifest, network recovery from `(owner, ns)`, compaction. **Per-DB store isolation** (`store_dir/<key>/` + read-fallback + traversal-guarded key): each DB's pages/GC are physically scoped so one DB's compaction can't delete another's dedup'd pages (fixed the `file is not a database` corruption; rolled + validated live). Backs the registry (per-shard DBs). |
| **Compute** | BUILT | ONE unified WASM runtime; per-program capability grant (deterministic subset vs full); determinism boundary = clock/random, not sql/obj; consensus clock; invoke returns committed bytes. |
| **Coordination (HeadRegistry)** | BUILT | **`2^shard_bits` GOVERNED, dynamically-resizable** shards (online power-of-two split/merge, drain+GC), each a **per-shard CraftSQL DB** (`heads` table), type-in-seed, K=3 **row-level** replication (push-on-write, merge-on-takeover), owner-signed LWW CRDT — **no attestation**, **native** write validation. Writer election over the **converged census** (scales with the writer set, not the size-5 active view); state migrates to follow the election on membership change; enumeration loops **O(held)**; readiness-gated. DB roots + manifests ride this; DHT KIND_ROOT/KIND_MANIFEST retired. **Read verification (DONE + live 2026-07-12):** the owner ed25519 signature now TRAVELS with each head (`HeadEntry.signature`, a `sig BLOB` col) and is re-verified at all three trust boundaries — replication merge (`sql_merge`), local read (`sql_resolve`), and the cross-node resolve RPC (`Resolved` carries the signed entry, caller re-verifies) — closing the trust-on-announce read gap so a forged/compromised replica can neither propagate (a forged high-version PushState is dropped, not version-guard-overwriting the honest head) nor serve an unsigned head. Idempotent `ADD COLUMN` migration (no regshards wipe). Rolled simultaneously (wire-incompatible); live-validated by a cross-node read of a freshly-signed DB-root head. |
| **Governance** | BUILT | Governor multisig (k-of-n), governance chain head, registry's executing program cid resolved *through* governance (upgradeable), native default that self-starts a fresh network. Governed `SetConfig` drives the registry `shard_bits`. |
| **Encryption** | BUILT (phases 1–4) | `cipher` crate (XChaCha20-Poly1305 + Umbral PRE), private files + private DBs, owner-only decryption proven by tests. Sharing (phase 5) is design-only. |
| **Performance / memory** | STABLE | Deploy 10s→40ms; invoke tail 26s→<250ms (fire-and-forget writes + sidecar-first opens); ~11 commits/sec/DB sequential. Hub-node RSS bounded by jemalloc (glibc-arena bloat fixed, ~13x cut; ~100–530 MB/node on the fleet). Census convergence reliably fast (~3.3s; periodic-epidemic safety net). |
| **Dashboard** | BUILT | Registry/Governance tabs, network-wide registry entries browser. |

**Fleet:** 4 Hetzner nodes (`zeph`/`zeph2`/`zeph3`/`zeph4` on one box), all rolled + healthy. The Mac node (sole governance governor) is stopped by choice — spin it up on demand for governance ops; do not reassign the governor. Validated on hardware: cross-node resolve, offline-owner resolve, DB-root-on-registry (survives restart), fast deploys, census election consistent 6→19 nodes with state following the election.

**Key commit evidence:** transport substrate — mux `dd0b7d1`/`3535f63`…`e2a1292` (cleanup `599f9b5`), choke `8bf7bc2`/`c7b63c3`/`5b3dd9b`, offer/grant `d540134`/`191d83c`; registry — census election `50f34ea`, state migration `a0d83f5`/`769cf93`, dynamic shards `9d29713`/`9b5d538`/`4abf6a5`, SQL-backed `376daab`, O(held) `f4db195`/`1a55f00`, readiness `402f26d`; memory `0ed4082`/`be9588b`/`9779416`; census hardening `3e4dcf4`; deploy gate `24eed4d`; compute 0–4 `1e9a9ba`…`43d2ebf`; encryption end-to-end.

---

## 3. Reconciliation matrix (spec-vs-code)

### 3.1 Confirmed built (verified in code + commits)
Transport substrate (mux/choke/offer-grant, §2); compute 0–4; DB-roots-on-registry (`57574f0`) + DHT retirement (`af5958e`); publish fire-and-forget (`fcc07e7`); registry census-election/dynamic-sharding/SQL-backed/O(held)/state-migration (§2 commits); tail-latency fix (`4e3b794`); config registry (`GovAction::SetConfig` consumed, drives `shard_bits`); encryption end-to-end. All CONFIRMED.

### 3.2 Real gaps — specced, not (fully) built (deferred by design unless noted)
- **Membership death-detection [K10] — DONE + LIVE (2026-07-12).** Full SWIM active detection: per-member incarnation + Alive/Suspect/Dead ride the gossiped member map (SWIM merge ordering), indirect PING-REQ rules out one-hop blips, suspect→dead promotion, refutation-by-incarnation (also handles restarted-node rejoin), Dead census-excluded immediately + shown "down" fleet-wide. Deaths converge in ~seconds via gossip instead of the old ~30-120s TTL aging. Rolled simultaneously (wire-incompatible) + live-validated (kill→Dead ~35s; restart→rejoin).
- Verification [K6] + attestation [K7] — **DONE + LIVE.** PDP / storage receipts (M3) [K5] — redirected to a lattice-LHS crypto milestone (sound PDP needs an asymmetric binary-field homomorphic signature; the vtags approach was forgeable — see the standalone `../binfield-lhs` design pass). Crypto-shred = **single-key deletion** (destroy your own non-re-derivable key → other nodes hold useless ciphertext); best-effort BY NATURE (a shared key can't be retracted; a future cipher break recovers captured ciphertext) — inherent, not a K4 gap.
- ~~`random` capability declared but no host fn bound [K2] — bind or drop.~~ **DONE 2026-07-14** — bound (chose bind: COMPUTE_EXECUTION_DESIGN puts `random` in the app profile alongside `wall_clock`). `random(out,len)` fills OS-CSPRNG bytes (≤64 KiB), granted in `full()` ONLY — the deterministic + verifier profiles deny it (tested: a deterministic-profile module importing `random` fails to instantiate), so consensus/verified programs still can't observe randomness.
- ~~File segmentation / K=32~~ **DONE + LIVE 2026-07-15 (Model B):** `publish_file` chunks into ≤8 MiB segments, each its own K=32 generation + cid, listed in `Manifest::File.segments`; `fetch_file` concatenates; `fetch_file_range` reads only covering segments; per-segment independent repair; block-level dedup. Wire-compatible (manifest-shape only), rolled to all 4 nodes, live-validated (20 MB → 3 segments, cross-node byte-identical). Follow-ups: private-file segmentation (**chunk-then-encrypt** — seal each plaintext segment independently under one DEK, so per-segment integrity survives → streaming works; NOT encrypt-then-chunk, whose whole-file AEAD tag blocks partial verify), streaming prefetch. — Still open: SIMD GF(2⁸) (scalar today); ephemeral ConsumeMode (aliased to Drop); `SIGNED_WRITE`/RPC-write CAS/delegation absent from the sql crate.
- General anchor dispatcher [K1] — no governed-WASM program exists today (registry validation went native), so no plural anchor table is needed yet.
- **Registry `sig`-column migration gap (found 2026-07-14).** `deploy` on the live `zeph` node fails `sqlite: table heads has no column named sig` while the SAME binary deploys fine on `zeph2` — a per-shard coverage gap in the read-verification `sig BLOB` idempotent `ADD COLUMN` migration (2026-07-12): a shard `heads` DB that predates the column, and whose node was the writer, wasn't migrated on open. Not an attestation issue (surfaced while smoke-testing owner-signed attestation). Fix: ensure the `ADD COLUMN` migration runs on EVERY shard DB open (incl. writer-elected takeovers), not only newly-created ones.

### 3.3 Genuine drifts — code diverged from spec detail (decide: fix doc or code)
- ~~**Writer election docstring**~~ **RESOLVED 2026-07-15 (doc fix).** Re-audit: `headreg.rs` was ALREADY correct (docstring says the stable K-replica set `min hash(rtype‖shard‖id)`, NO epoch term, + `replicas[epoch % K]` rotation). The stale spec was `REGISTRY_DESIGN.md:74` (`min hash(shard‖epoch‖id)`) — now fixed to match the impl.
- ~~**SQL live pages are not individually erasure-coded**~~ **RESOLVED 2026-07-15 (doc fix — code is CORRECT).** The code (`gen.rs`) does generation-level coding (K=8 pages/gen → k=8/n=32), which MATCHES foundation §427 and is the sound design — per-page (k=1) would be ~18× overhead vs ~4×. A page inherits its generation's full erasure redundancy AND HealthScan repair (`gen.rs:3`, `obj/lib.rs:526` keeps "system generation (CraftSQL)" alive like a pinned object), so churn resistance = a standalone pinned object; the durability *unit* is the generation. Only §392's summary line was imprecise (read as per-page) — clarified to point at §427.
- ~~**SQL index nodes + root are plaintext**~~ **RESOLVED 2026-07-15 (Option A — code fixed).** Private-DB **index nodes are now sealed** under the DB DEK (`pager.rs` `seal_index`/`open_index`, deterministic → index dedup preserved), so a holder no longer sees the page CIDs or tree structure (ENCRYPTION §7). The DEK is threaded through every index-traversal path (`open`/`load_subtree`/`reachable`/`sync_reachable`/sweep/compaction), unwrapped from the root's plaintext `wrapped_dek`. **Scope correction during impl:** the root HEADER stays plaintext + byte-identical (NOT restructured) — reordering it would have broken the postcard format of ALL public roots incl. **the registry shards** (they open public via `sql.open`), which a roll must not do. `root_cid` now points at a sealed node → leaks no structure; only `page_count`/`depth` remain visible (a coarse size hint a holder already infers from stored pieces). No migration — old *private* DBs break (accepted); public DBs incl. the registry are wire-unchanged. Residual: deterministic sealing still lets a holder see *which* nodes changed across commits (revisit with non-deterministic nodes if needed). Tested: private index node is ciphertext (page CIDs absent), owner reads back, foreign reader can't recover plaintext. **Rolled to all 4 Hetzner nodes 2026-07-15** (staggered; gate 🟢): each reconverged to 3 peers, 0 restarts, 0 panics, and **0 SQL/index errors** post-roll — the public registry shards opened fine, confirming the root-header-unchanged design kept them wire-safe. Commit `efb55fc`.
- ~~**HealthScan reads provider records + liveness filter, not live availability probes**~~ **DONE 2026-07-14 [K8]** — the elected scan now VERIFIES each census-live holder with an `AvailabilityProbe` (concurrent over the mux, `buffer_unordered(16)`), using the probed count as `have` (record = fallback on timeout). Closes the "alive-but-evicted" gap (silent eviction §31 + 48h record TTL) the census can't see. Storm-safe: bounded by the `should_scan` election (one elected cid per node) + per-probe timeout — NOT the reverted O(cids×providers) sequential-dial sweep.

### 3.4 Doc contradictions — the fix worklist (**RE-AUDITED + reconciled 2026-07-15**)
A 4-agent re-audit against current code found this worklist was itself badly overstated: **most flagged docs were already reconciled** (the code-local ones entirely). Status after the 2026-07-15 sweep:
| Doc / location | Prior claim flagged | Verdict + action |
|---|---|---|
| **REGISTRY_DESIGN** | attested / committee / quorum / PDA-CraftSQL-DB, "no publisher online → reads from durable storage" | **WAS the real offender** — top banner + §2.1 were correct but the BODY (§1/§7/§8/§10) was un-bannered stale. **RECONCILED in place 2026-07-15** (§1/§2/§5/§7/§8/§10 + banners → owner-signed CRDT, native validation, per-shard SQL, live-writer resolve). |
| **Foundation §62 A1/A2/A3-b1** | attestation-quorum authority, owner-or-attestation heads | Stale but **already inline-annotated** [SUPERSEDED] (07-07) — reader is warned. A3-b1 correction's "not a CraftSQL DB" over-correction **refined 2026-07-15** (heads DO live in per-shard CraftSQL DBs). A3-b2 (DB roots on registry) IS the as-built. |
| **Foundation Part F** §408/§411, §34/§37/§397 | synchronous CAS / `WRITE_CONFLICT`, Pkarr/DHT/SWIM publication | Stale but **already inline-annotated** [STALE]. Fixed the residual **`RootStore` trait doc-comment** (`db.rs`) that still claimed CAS/`Conflict`. |
| **CRAFTCOM_DESIGN** §5/§10 | `craft_*` names, single `craft_clock` | **Already-correct** (unprefixed + clock/wall_clock split). **Completed the §5 table 2026-07-15** — added the 5 omitted host fns (`ed25519_verify`/`random`/`verify`/`attest`/`pre_grant`). |
| **ENCRYPTION §8** | guaranteed self-executing crypto-shred | **Already-correct** (honest best-effort). **Tightened 2026-07-15**: noted the published-object caveat (capsule persists in the envelope on holders; PRE key is seed-re-derivable → per-object/identity shred is itself best-effort for already-published objects). |
| **GOVERNANCE §4/§6** | PDA-registry program + committee attests | §4/§6 **already-correct** (rewritten 07-08). **Fixed §2 2026-07-15** — the stale "attestation committee" table → the current verification-vs-governance / chain-fold model. |
| **routing crate prose / obj docstring / internal comments** | tracker-as-backend; publish-blocks-until-K-acks; headreg 3s / `crate::Runtime` / `app/<name>` | **All already-correct** — code is annotated (DHT sole backend, legacy names), the obj docstring already says fire-and-forget, headreg says 8s, `crate::Runtime`/`app/<name>` are gone. §3.4 was stale on these. |

**Residual (low priority, deliberately left):** the foundation's struck-through historical text (bannered, reader is warned) — keep as rationale, not deleted. REGISTRY §9 SUPERSEDED banner still says "postcard blob" (blanket-corrected by the authoritative UPDATE banner; cosmetic).

### 3.5 Progress-file hygiene
Tick the done boxes (compute 0–4, routing-trait trim, DHT phase-2 cleanup). **`apps/guestbook-wasm/` is untracked** — the real-app demo exists but was never committed. Still-true gaps: content durability <8 nodes, read-caching deferred, per-node registry views.

---

## 4. On hold — deferred by design
- The verification layer (Track B / VERIFICATION_DESIGN); verifier re-run reproducibility (persist `now` in the request).
- CraftCOM future: app versioning (name→CID head) + app-store-as-a-catalog-app — kept out of the node.
- Writer leases; read-caching; auto-recovery/compaction triggers; the `releasing` churn-cleanup loop.
- Sharing via proxy re-encryption (ENCRYPTION phase 5); crypto-shred Tier-0.

---

## 5. Known architectural constraints
- **Registry control-plane scaling — RESOLVED.** The historical ceiling (election over the fixed size-5 HyParView active view → ~6 writers regardless of cluster size, and divergence past ~6 nodes) is gone: election runs over the converged census (`eligible` grew 6→19 in a live test, consistent across nodes), state migrates to follow the election on membership change, the shard count is governed/elastic [K9], and the registry is per-shard SQL with O(held) loops. The control plane now scales with the writer set. **Membership gossip is now sub-linear too — DONE + live [S1]:** digest/delta gossip made it O(1) steady-state (a 32-byte set-hash exchange when in sync) / O(Δ) under churn / O(N) reconcile only on a real digest mismatch, replacing the old full-map-every-5s O(N)/round; liveness is now the converged SWIM state, not a `last_heard` TTL. **Remaining scale work** (not a ceiling): churn damping (hysteresis) under sustained heavy churn; coalescing head publishes at very high write rates. The data plane (DB pages, content) is genuinely share-nothing.
- **Failure detection** — SWIM Suspect/Dead dissemination + indirect PING-REQ is DONE + live [K10]; deaths converge in ~seconds via gossip. The gossip itself is now O(1)/O(Δ) via digest/delta [S1] — DONE + live.
- **Per-DB single-writer** — one DB is capped at its writer (~11/s here); scale is across many DBs, never within one.
- **Aggregate throughput is unmeasured** — all figures are sequential single-DB latency, not a scaling curve.

---

## 6. Missing kernel primitives — the bounded backlog

The minimal-kernel bet is largely won: the substrate (account model + unified runtime + storage + governance + transport) is complete, and most "features" reduce to **governed protocol programs** (reputation, tracking, catalogs, app store, versioning, verification *coordination*) — no kernel change. What remains is a bounded set of primitives the kernel must expose first. **Litmus:** new policy over existing primitives → protocol program; needs a new capability/wire/substrate mechanic → kernel first.

| # | Primitive | Kind | Unlocks | Status |
|---|---|---|---|---|
| **K1** | Anchor dispatcher + config registry | substrate | governed protocol programs; governed config | **Config half DONE** (`SetConfig` consumed → `shard_bits`). Anchor half **reframed** — registry validation went native; a plural anchor table is only needed when a genuinely governed-WASM program appears. |
| **K2** | `random` host fn | capability | full-profile apps needing randomness | **DONE + tested 2026-07-14.** Bound `random(out,len)` → OS-CSPRNG bytes (≤64 KiB), app (`full`) profile only; deterministic + verifier profiles deny it (a consensus/verified program can't observe randomness). |
| **K3** | Proxy re-encryption sharing (PRE) | capability | **sharing / grants** (the whole substrate) | **DONE + LIVE 2026-07-15.** `cipher` Umbral (a THRESHOLD PRE) ops `grant`/`reencrypt`/`decrypt_granted` (M-of-N, threshold built in → K4 not needed) + `Capability::Pre` + a runtime-mediated **`pre_grant`** host fn: the backend (`CraftBackend::pre_rekey`) derives THIS identity's PRE key and returns the blind re-encryption fragments — the app never sees the secret (`ENCRYPTION §13`). **Design-corrected against ENCRYPTION §9b/§13** (the original "expose rekey+reencrypt raw-key host fns" plan was wrong): reencrypt is **pure WASM** (no host fn; its threshold trust maps onto attestation), the grant RECORD is a row in the **owner's own CraftSQL DB** (existing `sql_execute` — NOT a new registry record, NOT the single-writer path: a grant is owner-authored + bilateral, no contention), and only the key-touching `generate_kfrags` is a host fn. Rolled staggered to all 4 Hetzner nodes (additive/local, wire-unchanged), each reconverged clean. Live-path integration test proves pre_grant → real fragments → reencrypt → recipient decrypts. |
| **K4 — DEFERRED (demoted 2026-07-14)** | Threshold secret-sharing (split/combine/destroy) | capability | generic k-of-n secret **escrow / recovery** only | Both original justifications dissolved: **crypto-shred** = single-key deletion (destroy your own non-re-derivable key → others' ciphertext is useless; needs no threshold — and threshold sharing makes shred *weaker* + recovery *easier*, the opposite of the goal); **sharing** = K3/Umbral (threshold PRE, built-in M-of-N). Threshold *signing* = FROST at chain boundaries, not this. Residual use = social key recovery / arbitrary-secret escrow — arguably app-level. **Build only if a concrete escrow need appears.** |
| **K5** | PDP challenge/response (+ holder proof over pieces) | wire+storage | storage proofs → reputation; M3 | Holder proof relates to vtags; reputation is policy above it. |
| **K6** | **Verification** — cross-node re-execution + signed verdict; app-declared threshold `k` (1/2/3) | wire | compute **consistency** (is the output correct?) | **DONE + LIVE 2026-07-13.** Full primitive in `zeph-com` (`verify_locally`, the `verify` capability/host ABI, the dumb `Board` CRDT, the cooldown `Verifier` scheduler + certificate, `produce`, `VerifyBackend`) + a noded `BoardService` (gossips/merges over `tag::BOARD`, a verifier loop that re-runs published programs, and the `verify()` producer path post→await). Rolled staggered (additive tag → mixed-version-safe); live-validated: an app calling `verify()` on one node was confirmed by another (`[01]`). Interchangeable open verifiers; higher `k` = more independent re-runs. |
| **K7** | **Attestation** — user-defined named-quorum **authority** ("set up your own consensus") | wire | app-scoped authority; policy/grant sign-off | **DONE + LIVE 2026-07-14.** Governance GENERALIZED + UNIFIED: `Quorum`/`Attestation`/`QuorumChain` in `zeph-com` are the shared substrate, and `GovernanceSet = Quorum` (wire-identical) — **network governance is now a literal instance**, no duplicated k-of-n. The `attest` capability/host fn + `AttestBackend` + a per-program `AttestStore` (quorum chains) + the `attest-*` control-plane CLI (Package A: manual cosign; `attest()` = `is_authorized`). **GLOBAL like governance (built + live 2026-07-14):** each program's `QuorumChain` publishes + pulls cross-node by the SAME anti-entropy `GovernanceChainStore` uses (`obj.publish_system` + `announce_app(attest-chain-<program_cid>, seq+1)` / `resolve_app` + adopt-longest-valid-sharing-genesis), and `is_authorized`/`attest()` sync-first — so ANY node answers, not just the collector. Rolled staggered (additive: new per-program DHT name + obj publish; `QuorumChain` wire unchanged). **OWNER-AUTHENTICATED genesis (built + live 2026-07-14):** the genesis quorum is signed by the program's registered **owner** bound to the program cid (an `AttestedChain{owner, owner_sig, chain}` envelope keyed by (owner, program), head `attest-chain-<owner>-<program>`); a node adopts a fetched chain only if that owner signature verifies AND the envelope owner equals the owner it resolved from the (owner-signed) registry — so a malicious peer can only ever publish a quorum under its OWN key, closing the trust-on-fetch hole. The owner fed to `attest()` is SERVER-resolved from the registry (never caller-supplied → an invoker can't self-authorize; raw-cid/remote invoke → attest UNAVAILABLE). This makes attestation's genesis trust root **as strong as governance's** (governance pins genesis from local config; attestation pins it to the program's owner, reusing the registry read verification). Adversarial security review found no bypass. Live-validated 3 ways on non-collector nodes: (a) owner-signed global propagation (zeph3/zeph4 verify + authorize); (b) anti-forgery — a different owner's self-signed quorum authorizes nothing under the real owner; (c) full invoke-by-name → `[01]`/`[00]`, raw-cid → `[ff]` UNAVAILABLE. Distinct from K6 (authority, not consistency); the two compose in the program. Deferred: member policy-program auto-signing. See `VERIFICATION_ATTESTATION_MODEL.md`. |
| **K8** | Issue `AvailabilityProbe`s from the health scan | wire (half-built) | verified availability counts | **DONE + tested 2026-07-14.** The elected scan issues the probe (concurrent, election-bounded) and counts VERIFIED holdings, not stale record claims — catches alive-but-evicted holders. Isolated test: a "ghost" inflated record + evicted holders → the scan flags the deficit only because it probes (fails if the probe is neutered). Also the issue-and-collect path PDP [K5] extends. |
| **K9** | Dynamic sharding (split/merge, rebalance) | substrate | elastic registry scale | **DONE + PROVEN LIVE.** Governed `shard_bits`; low-bit routing makes split/merge LOCAL; online reshard with drain window + old-generation GC; grow (8→9) and shrink (9→8) proven cross-node. |
| **K10** | SWIM dissemination (Suspect/Dead gossip + indirect PING-REQ) | substrate | robust failure detection at scale | **DONE + LIVE.** Both halves built: the converged census (JOIN, fast-converging ~3.3s) AND active DEATH detection — per-member incarnation + Alive/Suspect/Dead in the gossiped member map (SWIM merge ordering), indirect PING-REQ (K helpers rule out one-hop blips), suspect→dead promotion, refutation-by-incarnation (also handles restarted-node rejoin), Dead census-excluded immediately + shown "down" fleet-wide. Deaths converge in ~seconds via gossip, not the old ~30-120s TTL. Rolled + live-validated 2026-07-12 (kill→Suspect→Dead in ~35s, eligible 4→3; restart→rejoin). The census gossip it rides is now O(1)/O(Δ) via digest/delta [S1] — DONE + live. |

**By cost:** capabilities K2–K4 cheapest (host fns over in-tree crypto); wire K5–K8 medium (K6 verification and K7 attestation are **distinct** primitives composed by programs, not one generalizing the other — K7 lifts the already-built governance substrate, so it is closer to a refactor than a new mechanism); substrate K1/K9/K10 deepest. K9 + K10 done; K1 config-half done. Once the K1 anchor half lands (if a governed-WASM program appears), reputation/tracking/catalog/app-store are pure protocol-program work.

---

## 7. Roadmap

Cross-references kernel primitives as **[Kn]**.

**Tier 0 — reconciliation & hygiene (now; cheap, low-risk):**
- Doc-reconciliation sweep of the stale *design* docs in §3.4 — purge attested/committee/tracker/Pkarr/fixed-shard framing; fix ENCRYPTION §8's impossible "guaranteed shred"; fix the writer-election docstring.
- Hygiene: ~~bind-or-drop `random` [K2]~~ (DONE — bound, app-profile only); commit `guestbook-wasm`; tick the done boxes.

**Tier 1 — registry scaling ceiling — ✅ COMPLETE:** census election + state migration, dynamic sharding [K9], SQL-backed registry + O(held) loops, and digest/delta membership gossip [S1] (O(1) steady-state) all landed and proven live (§2, §5). Residual (secondary): coalesce head publishes at very high write rates; churn hysteresis.

**Tier 2 — extensibility & robustness:**
- **SWIM Suspect/Dead dissemination [K10] — ✅ DONE + LIVE (2026-07-12).** Both halves shipped (census JOIN + active DEATH detection); deaths converge in ~seconds via gossip.
- Anchor dispatcher [K1] — only when a governed-WASM protocol program appears.
- App-name resolve cache; ~~`AvailabilityProbe` issuance [K8]~~ (DONE — verified availability in the elected scan).

**Tier 3 — prove the product thesis:**
- Federated app demo (multi-owner, enumerated via the registry directory) + cross-node app-DB read path.
- **Sharing [K3] — the encrypted-grants substrate. ✅ DONE + LIVE 2026-07-15.** `cipher` Umbral (threshold PRE) ops + `Capability::Pre` + runtime-mediated `pre_grant` (backend-derived key; app never sees the secret). Design-corrected: reencrypt = pure WASM, grant record = owner-DB SQL row (not registry/single-writer), only `generate_kfrags` is a host fn. Rolled to all 4 nodes. Delegated + threshold in one primitive — K4 not needed.
- **Prove the scaling win with numbers — DONE (single-node + multi-node benches).** (1) *Per-row:* `bench_register_resolve_latency_vs_row_count` (`headreg.rs`, `#[ignore]`): resolve p50 FLAT ~74–121µs as heads grow 150→4050 (27×; indexed SELECT is O(log n) — the per-row win over the old whole-shard blobs), register p50 flat ~25–29ms (commit-bound), all 256 shards fill. (2) *Multi-node O(held):* `bench_registry_multinode_scaling` (`headreg.rs`, `#[ignore]`) drives the real election (`replicas`/`writer_of`) over N-node censuses — held/node = total·min(K,N)/N shrinks linearly (576 @N=4, 72 @32, 9 @256; ~85× less per-node status/enumeration/reshard work than a lone node at N=256), writers/node = total/N, load-balanced (≤1.22× hotspot for N≤32). Live anchor: the fleet reports `eligible=4` and the seed writes 41/256 program shards — a ~1/N share, the same model the bench proves.

**Tier 4 — trust primitives:**
- **Verification [K6] + attestation [K7] — DONE + LIVE** (two distinct primitives composed by programs — consistency vs authority). PDP [K5] + reputation — redirected to the lattice-LHS crypto milestone (sound PDP needs an asymmetric binary-field homomorphic signature; standalone `../binfield-lhs`). Threshold shares [K4] — **deferred/demoted** (crypto-shred = single-key deletion; sharing = K3/Umbral; signing = FROST — see §6 K4).

---

## 8. Recommended next step

The registry scaling ceiling and the transport substrate are done and live, so the highest-value items are:

With **K3 sharing now DONE + LIVE (2026-07-15)**, the buildable kernel primitives are essentially complete (K2/K3/K6/K7/K8/K9/K10 done + live; K5 PDP is cryptographer-gated on the lattice-LHS milestone; K4 deferred). The highest-value items now:

1. **Finish the Tier-0 doc-reconciliation sweep (§3.4) — IN PROGRESS 2026-07-15.** RE-AUDIT FINDING: the *code-local* drifts (obj docstring, routing prose, headreg-timeout + `crate::Runtime`/`app.<name>` internal comments) are **already fixed** — §3.4 was itself stale on those. The genuinely-open drift is the **big ecosystem design docs**: foundation §62/Part F, GOVERNANCE §4/§6, CRAFTCOM_DESIGN §5/§10, ENCRYPTION §8, REGISTRY_DESIGN — still describing attestation-committee/tracker/Pkarr/CAS/fixed-shard models the code replaced. Low-risk, high-leverage. (Also §3.5 hygiene: `apps/guestbook-wasm/` is untracked — commit it.)
2. ~~**File segmentation**~~ **DONE + LIVE 2026-07-15 (Model B — segment = sub-cid).** Design-check chose the IPFS-UnixFS DAG over the doc's §80–92 in-piece-index model: `Manifest::File.segments` lists per-segment cids (each an independent K=32 generation), which turned out **wire-compatible** (only the manifest shape changed — no piece/ALPN change), reused all per-cid repair/health/distribute machinery, and delivered §224's block-level dedup + range reads. Rolled staggered to all 4 nodes, live-validated cross-node. CRAFTOBJ §80–92/§224 reconciled to Model B. Follow-ups: private-file segmentation (**chunk-then-encrypt** — per-segment seal for streamable integrity), streaming prefetch, CLI range-read.
3. Then: the first **app on the substrate** (private file-share exercising K3); PDP [K5] cryptographer-gated (`../binfield-lhs`); the funded-tier/economic layer (unbuilt); the compute lift (rich WASI/POSIX → OSes); K1 anchor half if/when a governed-WASM program appears. (K4 deferred; K6/K7/K9/K10 done + live.)
